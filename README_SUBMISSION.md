# 天池 Agent 提交目录

- 最新提交压缩包：`demo_agent_0529_latest_optimized_20260606_2324.zip`
- 新 0529 提交结构：压缩包内顶层为 `demo/agent/`，符合 `demo_docs_release_20260529 (2)/docs/04-提交方式.md`。
- 解压源码：`agent/`
- 入口：`agent/model_decision_service.py`
- 模型：不在 Agent 内写死；由官方 server / 环境侧统一指定。
- 收益优先优化：默认 `query_cargo k=120` 降低扫描耗时；明确安全正收益货源会跳过 LLM review 直接接单；LLM 单次默认超时约 40 秒。
- 泛化修复：仅在偏好明确出现禁入/围栏/经纬度范围时接受 LLM 地理围栏，避免把每日回家误扩展成全天地理限制；每日回家窗口按距离提前计算 `leave_by`，避免远距离回家迟到。
- LLM 响应优化：保留官方 `model_chat_completion` 调用，但压缩 planner 输入，只在收益/偏好权衡不确定时调用动作 LLM，降低 60 秒 action 超时风险。
- 日历工具：`agent/tools/task_calendar_tool.py`，支持固定任务、禁动作窗口、月度休息、月度到访、区域货源订单日的滚动调度；每日连续休息会预排为固定晚间休息段，非日程窗口优先接单赚钱。
- 日程泛化校验：只有偏好文本明确出现熟货、临时任务、月度到访、区域货源、固定日期等语义时，才接受 LLM 解析出的对应日程，避免幻觉日程长期压住市场。
- 偏好优先接单：先选完全符合偏好的候选；如果外单扣除回到偏好范围的空驶成本和软罚分后明显更赚钱，才允许进入候选，由收益/罚分权衡选择。
- 固定日期泛化：支持“盘库/清库存/停一趟”等固定日期到访任务，避免日程被收益单压掉。
- 地域限制修复：清理“正向工作范围/地理围栏城市”被误识别为禁入区域的问题；默认不加载跨运行持久化工具记忆，避免本地测试残留规则污染隐藏司机。
- 休息与停留修复：固定日期任务若要求停留，会累计等待到满足时长；8 小时连续休息的候选保护提前到中午，避免午后长单吃掉当天休息窗口。
- 代码说明：`AGENT_CODE_EXPLANATION.md`
- 复盘学习文档：`AGENT_IMPLEMENTATION_REVIEW.md`，整理核心代码、实现思路、主要不足、为什么收益不理想，以及如果重做应该怎么设计。
- 最新回归结果：
  - `0509 D001-D010`：`/tmp/tianchi_agent_0509_all_geo_return_profit_20260606_231956/summary.json`，总净收益 `191934.34`，总偏好扣分 `9290`，总 token `121983`，`step=-1` 为 `0`。
  - `0529 D001/D002`：`/tmp/tianchi_agent_0529_d001_d002_geo_return_profit_20260606_231956/summary.json`，总净收益 `38212.04`，总偏好扣分 `0`，总 token `48667`。
- 旧工作区备份：`/Users/bytedance/Desktop/天池agent_full_backup_20260606_141443`
